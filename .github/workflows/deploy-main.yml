name: Deploy to Production Server on main branch

on:
  push:
    branches:
      - main  # main 브랜치로 머지/푸시될 때만 실행

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST_MAIN }}
          username: ${{ secrets.EC2_USER_MAIN }}
          key: ${{ secrets.EC2_SSH_KEY_MAIN }}
          script: |
            # 프로젝트 디렉토리로 이동
            cd /home/ubuntu/services/Back_InternalApprovalSystem
            
            # Git 최신 코드 가져오기 (main 브랜치)
            echo "📥 Fetching latest code from main branch..."
            git fetch origin
            git checkout main
            git reset --hard origin/main
            
            # 최신 커밋 정보 출력
            echo "📌 Current commit: $(git log -1 --oneline)"
            
            # Node.js 의존성 설치 (항상 깨끗하게 재설치)
            echo "📦 Installing dependencies..."
            npm ci --production=false
            
            # 빌드 (필요시)
            echo "🔨 Building application..."
            npm run build || echo "Build skipped or failed"
            
            # PM2로 서버 재시작 (ecosystem.config.js 사용)
            echo "🚀 Restarting application with PM2..."
            pm2 restart back-internal-approval-system || pm2 start /home/ubuntu/services/ecosystem.config.js --only back-internal-approval-system
            
            # PM2 설정 저장
            pm2 save
            
            # 배포 상태 확인
            echo "✅ Deployment completed!"
            echo "📊 PM2 Status:"
            pm2 list
            pm2 logs back-internal-approval-system --lines 5 --nostream || true
